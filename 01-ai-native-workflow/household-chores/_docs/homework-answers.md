# Homework answers

1. **Coding agent:** Codex.
2. **Features:** create a chore with its owner and schedule; view open chores; filter by owner; mark a chore complete.
3. **Django app registration file:** `settings.py`.
4. **Backlog task 1:** Create the Django project and `chores` app; configure templates, database, and root URL routing.
5. **Development server command:** `uv run python manage.py runserver`.
6. **Test command:** `uv run python manage.py test`.

For this local implementation, the equivalent commands `python manage.py runserver` and `python manage.py test` work after installing `requirements.txt`.
