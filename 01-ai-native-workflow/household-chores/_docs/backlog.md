# Implementation backlog

1. Create the Django project and `chores` app; configure templates, database, and root URL routing.
2. Add the `Chore` model with validation and admin registration; create and apply migrations.
3. Build the dashboard for open chores, including assignee filtering and overdue state.
4. Build the add-chore form and success redirect.
5. Add a completion action that records the completion date.
6. Add tests for model state, filtering, form submission, and completion.
